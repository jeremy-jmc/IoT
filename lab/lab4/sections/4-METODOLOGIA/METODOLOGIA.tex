\section{Introducción a Comunicación LoRa (Comandos AT)}

La comunicación LoRa (Long Range) es una tecnología que permite la transmisión de datos a larga distancia con un bajo consumo de energía. Para la configuración de los dispositivos LoRa, como el RAK3172S, se utilizan comandos AT, los cuales permiten configurar parámetros como la frecuencia de operación, el modo de comunicación y otros ajustes esenciales.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/AT_command.png}
    \caption{Ejecución de comandos AT en el dispositivo RAK3172S}
    \label{fig:at_command}
\end{figure}

% TODO: desarrollo de la experiencia

\section{Carga de Código de Prueba a RAK3172S}

El código de prueba para el dispositivo RAK3172S fue cargado a través del IDE de Arduino, lo que permitió verificar la funcionalidad básica del dispositivo en cuanto a la comunicación y ejecución de comandos AT.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/codigo_prueba.png}
    \caption{Captura de pantalla mostrando la carga del código de prueba en RAK3172S}
    \label{fig:codigo_prueba}
\end{figure}

% TODO: desarrollo de la experiencia

\section{Implementación del sistema de comunicación P2P con LoRa}

\subsection{Transmisor}

Para la implementación de la comunicación P2P con LoRa, se configuraron dos dispositivos RAK3272S, uno como transmisor y otro como receptor. En el siguiente diagrama se muestran las conexiones y la implementación del transmisor.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/transmisor_setup.png}
    \caption{Configuración del transmisor RAK3172S}
    \label{fig:transmisor_setup}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/transmisor.jpg}
    \caption{Montaje del transmisor RAK3172S}
    \label{fig:transmisor_impl}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/transmisor_envio_mnsjs.png}
    \caption{Envío de mensaje LoRa desde el transmisor}
    \label{fig:transmisor_envio_mnsjs}
\end{figure}

\subsection{Receptor}

El receptor RAK3272S está configurado para recibir los mensajes enviados por el transmisor. A continuación se describe el proceso de recepción de mensajes LoRa.

% TODO: Receptor
% TODO: desarrollo de la experiencia

\section{Implementación del sistema de comunicación P2P con LoRa, integrado con Arduino y un sensor}

En esta etapa, se utilizó un sensor (MQ2) conectado al Arduino MEGA. Los datos del sensor fueron enviados al módulo RAK3272S a través de la comunicación UART.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/emisor_arduino.jpg}
    \caption{Montaje del Arduino MEGA con el sensor MQ2}
    \label{fig:emisor_arduino}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/receptor_arduino.jpg}
    \caption{Montaje del receptor Arduino conectado al RAK3272S}
    \label{fig:receptor_arduino}
\end{figure}

\begin{lstlisting}[style=cppstyle]
#define SENSOR_PIN A0       // Pin del sensor
#define RAK Serial1         // Puerto conectado al RAK (TX1/RX1)
#define BAUD_PC 9600        // Velocidad para PC
#define BAUD_RAK 115200     // Velocidad del RAK3172

void setup() {
  Serial.begin(BAUD_PC);    // Comunicacion con la PC
  RAK.begin(BAUD_RAK);      // Comunicacion con el RAK3172
  delay(1000);
  Serial.println("Inicio de transmision LoRa con datos del sensor...");
}

void loop() {
  int sensorValue = analogRead(SENSOR_PIN);  // Lectura (0 a 1023)
  Serial.print("Valor leido del sensor: ");
  Serial.println(sensorValue);

  // Convertir a hexadecimal y rellenar a 4 digitos con ceros
  String mensajeHex = String(sensorValue, HEX);
  while (mensajeHex.length() < 4) {
    mensajeHex = "0" + mensajeHex;
  }
  mensajeHex.toUpperCase();  // Opcional: para mantener todo en mayusculas

  // Enviar comando AT+PSEND=XXXX
  String comando = "AT+PSEND=" + mensajeHex + "\r\n";
  RAK.print(comando);

  Serial.print("Comando enviado al RAK: ");
  Serial.println(comando);

  // Leer respuesta del RAK
  unsigned long start = millis();
  while (millis() - start < 1000) {
    while (RAK.available()) {
      char c = RAK.read();
      Serial.write(c);
    }
  }

  delay(3000);  // Esperar antes de enviar de nuevo
}
\end{lstlisting}

% TODO: codigo y fotos

\section{Sistema IoT de alerta de fuga de gas}

En esta sección se implementa un sistema IoT para la detección de fugas de gas. El sistema está compuesto por un módulo detector y un módulo de alerta. A continuación, se muestra la implementación del módulo detector.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{./img/fuga_gas_impl.jpg}
    \caption{Implementación del módulo detector de fugas de gas}
    \label{fig:fuga_gas_impl}
\end{figure}

% TODO: Implementación de ambos sistemas y demostrar el funcionamiento de ambos.
% TODO: Mensajes enviados, en sus respectivas frecuencias.
% TODO: Los sistemas deben estar conectados a laptops diferentes, a fin de demostrar la comunicación inalámbrica.
% TODO: Diagrama de bloques y diagrama de flujo (en clase).
% TODO: Tomar fotos y grabar videos del funcionamiento para su informe.
  
\begin{figure}[H]
    \centering
    \begin{minted}[fontsize=\small]{text}
    flowchart TD
    START([Inicio])
    TEMP[Leer temperatura]
    GAS[Leer concentración de gas]
    TEMP_ALT{¿Temp > 50°C?}
    GAS_ALT{¿Gas > 80 ppm?}
    ALARM_TEMP[Activar LED rojo y buzzer (500ms ON / 1s OFF)]
    ALARM_GAS[Activar LED azul, buzzer (1.5s ON / 1s OFF), y servo 0°→180°]
    ENVIAR[Enviar mensaje por LoRa]
    ESPERAR[Esperar y repetir]

    START --> TEMP --> GAS --> TEMP_ALT
    TEMP_ALT -- Sí --> ALARM_TEMP --> ENVIAR --> ESPERAR
    TEMP_ALT -- No --> GAS_ALT
    GAS_ALT -- Sí --> ALARM_GAS --> ENVIAR --> ESPERAR
    GAS_ALT -- No --> ESPERAR
    \end{minted}
    \caption{Diagrama de flujo del sistema de detección y respuesta}
    \label{fig:bloques_fuga}
  \end{figure}
  
  \begin{figure}[H]
    \centering
    \begin{minted}[fontsize=\small]{text}
      flowchart TD
      subgraph Módulo Detector
      MQ2[Sensor de gas MQ2]
      LM35[Sensor de temperatura LM35]
      ARDUINO[Arduino MEGA 2560]
      LED_R[LED Rojo]
      LED_A[LED Azul]
      BUZZER[Buzzer]
      SERVO[Servo Motor]
      RAK_TX[RAK3272S - Transmisor]
      
      MQ2 --> ARDUINO
      LM35 --> ARDUINO
      ARDUINO --> LED_R
      ARDUINO --> LED_A
      ARDUINO --> BUZZER
      ARDUINO --> SERVO
      ARDUINO --> RAK_TX
      end
      
      subgraph Módulo Alerta
      RAK_RX[RAK3272S - Receptor]
      PC[Terminal Serial en PC]
      RAK_RX --> PC
      end
      
    \end{minted}
    \caption{Diagrama de bloques del sistema de alerta de fuga de gas}
    \label{fig:flujo_fuga}
\end{figure}
